#include <stdio.h>

/**
 * main - starting point of the code
 *
 * Return: Always returns 0 for success
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
