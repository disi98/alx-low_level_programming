#include <stdio.h>

/**
 * main - starting point of the code
 *
 * Returns: always returns 0 if success
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
